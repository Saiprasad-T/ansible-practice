- name: taking inputs from the user through terminal using prompts
  hosts: local
  connection: local
  vars_prompt:
  - name: username
    prompt: please enter your username
    private: false
  - name: password
    prompt: please enter your password
    private: true
  - tasks:
    ansible.builtin.debug:
      msg: "username is {{ username }} and password is {{ password }}"